# Core dependencies
anthropic==0.49.0
python-dotenv==1.1.0
Pillow==12.1.1
pystray==0.19.5
keyboard==0.13.5

# Everything SDK — Windows DLL (no pip package).
# Download Everything-SDK.zip from https://www.voidtools.com/support/everything/sdk/
# and place Everything64.dll (or Everything32.dll) in the project directory,
# or set EVERYTHING_DLL_PATH in .env.
